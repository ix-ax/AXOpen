## Changes
<!--
    Leave this comment intact. Immediately below this comment add a new entry:
    ---------------------------------
    ### {axopen-version}

    **New features:**
    -

    **Bug fixes:**
    -

    **Other:**
    -

    **Breaking changes:**
    -
    ---------------------------------

    Replace {axopen-version} with the `next-version:` value from
    axopen/GitVersion.yml. New entries go at the TOP, immediately below
    this comment. The /axopen-docs skill resorts the file semver-descending
    on every run.
-->

### 0.43.0

**Other:**
- Renamed showcase file `_Showcase.st` → `AxoCVIC_II.st` and realigned all `[!code-pascal[]]` references to the new filename (class-name convention).
- Renamed `AxoCVIC_II_Showcase.md` → `AxoCVIC_II.md` (class-name convention).
- Removed broken `AxoCVIC_II_Showcase2.md` reference (no Showcase2 ST exists).
- Added .NET TWIN, BLAZOR, HARDWARE tabs with source links.
- Fixed source links to point to `Inxton/axopen.templates` repo (branch `troublesense-integration`) and corrected .NET namespace to `AXOpen.Components.Desoutter.Tightening`.
- Removed obsolete `ComponentTemplate.md`.
