@namespace AXOpen.Data


@using AXSharp.Connector
@using Microsoft.AspNetCore.Components.Authorization;
@inherits RenderableComplexComponentBase<AxoDataExchangeBase>;
@inject IAlertDialogService _alertDialogService
@inject AuthenticationStateProvider _asp

<DataExchangeView Vm="_vm" Presentation="Command"></DataExchangeView>

@code
{
    private DataExchangeViewModel _vm => new DataExchangeViewModel { Model = this.Component, AlertDialogService = _alertDialogService, Asp = _asp };

    public override void AddToPolling(ITwinElement element, int pollingInterval = 250)
    {
        
    }
}

