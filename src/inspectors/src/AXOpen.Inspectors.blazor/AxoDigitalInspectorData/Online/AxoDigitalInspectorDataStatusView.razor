@namespace AXOpen.Inspectors
@using AXSharp.Connector
@using System.Globalization
@using AXSharp.Connector.Localizations
@inherits RenderableComplexComponentBase<AxoDigitalInspectorData>

<InspectorViewTemplate Data="Component"
                       Name="@getNameOrSymbol()"
                       Presentation="Display" />


@code {
    private string ID = "M" + Guid.NewGuid().ToString();

    public string getNameOrSymbol()
    {
        return string.IsNullOrEmpty(Component.AttributeName) ? Component.GetSymbolTail() : Component.Translate(Component.AttributeName, CultureInfo.CurrentUICulture);
    }

    public override void ConfigurePolling()
    {
        this.StartPolling(this.Component, 500);
    }
}

