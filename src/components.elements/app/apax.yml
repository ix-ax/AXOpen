name: "elementscomponents"
version        : '0.1.0'
type: app
targets:
  - "1500"
  - plcsim
  - llvm
variables:
  APAX_BUILD_ARGS:
    - "--debug" # Generate debug information for target "1500"
devDependencies:
  "@ix-ax/ax-sdk"        : '0.1.0'
dependencies:
  "@ix-ax/axopen.components.elements"        : '0.1.0'
scripts:
  download:
    - apax install -L
    - apax build
    - dotnet ixc
    - apax sld load --accept-security-disclaimer -t $AXTARGET -i
      $AXTARGETPLATFORMINPUT -r
installStrategy: strict
apaxVersion: 3.1.0
